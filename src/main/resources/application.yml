server:
  port: 8888

spring:
  application:
    name: CONFIG-SERVER

  cloud:
    config:
      server:
        git:
          uri: https://github.com/sharankumarrathod/config-repo.git
          default-label: main
          clone-on-start: true
